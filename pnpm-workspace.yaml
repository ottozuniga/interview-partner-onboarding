packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10 blocks postinstall scripts by default. These four need them:
# Prisma downloads/generates its query engine, esbuild fetches its binary.
onlyBuiltDependencies:
  - '@prisma/client'
  - '@prisma/engines'
  - prisma
  - esbuild
